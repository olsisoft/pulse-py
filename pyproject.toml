[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "streamflow-pulse-client"
version = "2.7.10"
description = "Official Python client for StreamFlow Pulse — AI Agent Platform"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.10"
authors = [
    {name = "Njong Michael", email = "mike.njo@gmail.com"},
]
keywords = ["pulse", "streamflow", "ai", "agents", "pipelines", "llm", "mcp"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "httpx>=0.27,<1.0",
]

[project.optional-dependencies]
# B-114 — duplex needs a WebSocket client. Optional so the core SDK stays
# httpx-only; install with `pip install streamflow-pulse-client[duplex]`.
duplex = [
    "websockets>=12,<16",
]
test = [
    "pytest>=7.4",
    "pytest-cov>=4.1",
    "pytest-asyncio>=0.23",
    "respx>=0.21",
    "websockets>=12,<16",
]
dev = [
    "streamflow-pulse-client[test]",
    "ruff>=0.6",
    "mypy>=1.10",
]

# Public, verifiable links ONLY (2026-08-13 credibility fix): the previous URLs
# pointed at the private monorepo — a public 404 next to an Apache-2.0 badge,
# which costs more trust than it buys. Source/Issues now point at the public
# distribution satellite, where the published sources actually live.
[project.urls]
Homepage = "https://streamflowmesh.io"
Documentation = "https://streamflowmesh.io"
Source = "https://github.com/olsisoft/pulse-py"
Issues = "https://github.com/olsisoft/pulse-py/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/pulse_client"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers --cov=pulse_client --cov-report=term-missing"
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "N"]
ignore = ["E501"]  # line length handled by formatter

[tool.mypy]
python_version = "3.10"
strict = true
warn_return_any = true
warn_unused_configs = true
warn_unused_ignores = true
